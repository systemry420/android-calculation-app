<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calculationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalculationApp">
        <activity android:name=".EditNote"
            android:label="Edit Note"/>
        <activity android:name=".Notepad" android:label="Notepad"/>
        <activity android:name=".Calculator" android:label="Calculator"/>
        <activity
            android:name=".WeightConverter"
            android:label="Weight Converter" />
        <activity
            android:name=".Multiplication"
            android:label="Multiplication Table" />
        <activity android:name=".MainActivity"
            android:label="IUL Mobile App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>